// $flow

import React from 'react';
import classNames from 'classnames/bind';

// $FlowIssue
import styles from './style.scss';

const cx: () => string = classNames.bind(styles);

type IProps = {

};

const HeaderButton = ({ }: IProps): void => {

};

export default HeaderButton;
